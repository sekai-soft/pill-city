services:
  .base:
    image: ghcr.io/sekai-soft/pill-city:latest
    environment:
      MONGODB_URI: mongodb://pill-city-resources:27017/db
      REDIS_URL: redis://pill-city-resources:6379
      API_DOMAIN: "api.pill.city"
      WEB_DOMAIN: "pill.city"
      JWT_ACCESS_TOKEN_EXPIRES: "1209600"
      OPEN_REGISTRATION: "false"
      ADMINS: "kt"
      OFFICIAL: "official"
      GHOST: "ghost"
    env_file:
      - env
    profiles: ["ignore"]

  pill-city:
    extends: .base
    container_name: pill-city
    restart: unless-stopped
    environment:
      PORT: "80"
    depends_on:
      - mongodb
      - redis
    network_mode: service:pill-city-ts
    labels:
      - com.centurylinklabs.watchtower.enable=true
  pill-city-ts:
    image: tailscale/tailscale:latest
    restart: unless-stopped
    hostname: pill-city
    environment:
      - TS_EXTRA_ARGS=--advertise-tags=tag:server
      - TS_STATE_DIR=/var/lib/tailscale
    env_file:
      - /home/nixos/env-tailscale
    volumes:
      - pill-city-ts:/var/lib/tailscale
      - /dev/net/tun:/dev/net/tun
    cap_add:
      - net_admin
      - sys_module
    labels:
      - com.centurylinklabs.watchtower.enable=true

  worker:
    extends: .base
    restart: unless-stopped
    entrypoint: ["celery", "-A", "pillcity.tasks", "worker", "--loglevel=INFO"]
    depends_on:
      - mongodb
      - redis
  beat:
    extends: .base
    restart: unless-stopped
    entrypoint: ["celery", "-A", "pillcity.tasks", "beat", "--loglevel=DEBUG", "--max-interval", "30"]
    depends_on:
      - mongodb
      - redis

  mongodb:
    image: mongo:8.2
    restart: unless-stopped
    container_name: mongodb
    volumes:
      - mongodb_data:/data/db
    healthcheck:
      test: echo 'db.runCommand("ping").ok' | mongo localhost:27017/test --quiet
      interval: 10s
      timeout: 10s
      retries: 10
    network_mode: service:resources-ts
    labels:
      - com.centurylinklabs.watchtower.enable=true
  redis:
    image: redis
    restart: unless-stopped
    container_name: redis
    volumes:
      - redis_data:/data
    healthcheck:
      test:
        - CMD
        - redis-cli
        - ping
      interval: 1s
      timeout: 3s
      retries: 30
    network_mode: service:resources-ts
    labels:
      - com.centurylinklabs.watchtower.enable=true
  resources-ts:
    image: tailscale/tailscale:latest
    restart: unless-stopped
    hostname: pill-city-resources
    environment:
      - TS_EXTRA_ARGS=--advertise-tags=tag:server
      - TS_STATE_DIR=/var/lib/tailscale
    env_file:
      - /home/nixos/env-tailscale
    volumes:
      - resources-ts:/var/lib/tailscale
      - /dev/net/tun:/dev/net/tun
    cap_add:
      - net_admin
      - sys_module
    labels:
      - com.centurylinklabs.watchtower.enable=true

volumes:
  pill-city-ts:
  mongodb_data:
  redis_data:
  resources-ts:
